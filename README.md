# openproject-kubernetes-docker
docker image intended to run in a kubernetes a cluster. database and redis get their own containers and connect via http. a volume mount can be used with concurrent storage
